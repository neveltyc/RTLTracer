"""RTLTracer — signal trace, driver and load analysis over an rtl-designdb database.

Every query targets the 18 v_* views of the RTLDebugDBKit v20 schema directly.
Python is a thin CLI wrapper; the SQL is the contract.
"""
__version__ = "0.1.0"
